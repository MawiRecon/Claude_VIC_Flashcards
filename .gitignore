# Local Claude Code settings / dev preview config (machine-local, not secret)
.claude/settings.local.json
.claude/launch.json

# Stale sample, no longer tracked (cards.json is the real manifest)
cards.csv

# OS cruft
.DS_Store
Thumbs.db
desktop.ini

# Secrets — never commit (the app uses a runtime PAT, never a file)
.env
.env.*
*.env
*.pem
*.key
id_rsa*
id_ed25519*
credentials.json
service-account*.json
*secret*
*apikey*
*api_key*
*.token
token.txt
